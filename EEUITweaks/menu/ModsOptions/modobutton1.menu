	button
	{
		enabled		"(modsInGameContext and (modOptionsIngameEnabled() == 1)) or ((not modsInGameContext) and (modOptionsStartupEnabled() == 1))"
		area		712 654 200 44
		bam			GUIBUTNT
		text 		"MODOPT_BUTTON"
		text style	"button"
		action
		"
			selectedOpt = 0
			Infinity_PushMenu('OPTIONS_MODS')
		"
	}
